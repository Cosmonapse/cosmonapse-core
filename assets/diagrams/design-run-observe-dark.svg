<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 920 342" width="920" height="342" role="img">
<style>
svg {
  --font-sans: system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif;
  --font-mono: ui-monospace, SFMono-Regular, 'SF Mono', Menlo, monospace;

  color-scheme: dark;

  /* ── Channel triplets ──────────────────────────────────────────────
     Every alpha-tinted colour in this sheet derives from one of these,
     so a theme only has to restate the triplet, not the 60 rgba() calls
     that consume it. Used as rgba(var(--fg-rgb), 0.06). */
  --fg-rgb: 255, 255, 255;
  --shadow-rgb: 0, 0, 0;
  --accent-rgb: 139, 92, 246;
  --accent2-rgb: 34, 211, 238;
  --accent3-rgb: 244, 114, 182;
  --engram-rgb: 167, 139, 250;
  --warn-rgb: 251, 191, 36;
  --ok-rgb: 74, 222, 128;

  /* Surfaces */
  --bg: #07080c;
  --bg-elev: #0c0e15;
  --bg-card: #0f111a;
  --bg-card-soft: rgba(15, 17, 26, 0.5);
  --bg-nav: rgba(7, 8, 12, 0.7);
  --bg-mobile: rgba(7, 8, 12, 0.96);
  --bg-menu: rgba(13, 15, 21, 0.96);
  --bg-tooltip: #12141d;

  /* Lines */
  --border: rgba(var(--fg-rgb), 0.06);
  --border-strong: rgba(var(--fg-rgb), 0.12);
  --border-hover: rgba(var(--fg-rgb), 0.18);
  --grid-line: rgba(var(--fg-rgb), 0.025);

  /* Type */
  --text: #e6e7ec;
  --text-dim: #9097a8;
  --text-faint: #5b6275;
  --brand-word-color: var(--text);

  /* Accents */
  --accent: #8b5cf6;
  --accent-2: #22d3ee;
  --accent-3: #f472b6;
  --accent-soft: #a78bfa;      /* accent legible as body text */
  --accent-text: #c4b5fd;      /* accent text on an accent wash */
  --accent2-soft: #67e8f9;
  --on-accent: #ffffff;        /* text on a *solid* accent fill */
  --ok: #86efac;
  --ok-strong: #4ade80;
  --warn: #fbbf24;
  --glow: rgba(var(--accent-rgb), 0.35);

  /* Buttons */
  --btn-primary-bg: #ffffff;
  --btn-primary-fg: #07080c;
  --btn-primary-bg-hover: #e6e7ec;

  /* Code surface + syntax tokens */
  --code-bg-1: rgba(15, 17, 26, 0.9);
  --code-bg-2: rgba(12, 14, 21, 0.9);
  --code-text: #d4d7e0;
  --tk-kw: #c084fc;
  --tk-fn: #67e8f9;
  --tk-str: #86efac;
  --tk-cm: #5b6275;
  --tk-num: #fbbf24;
  --tk-op: #f472b6;

  /* Gradient wordmarks */
  --grad-1: #ffffff;
  --grad-2: #a78bfa;
  --grad-3: #67e8f9;

  /* Elevation */
  --shadow-lg: 0 30px 80px -20px rgba(var(--shadow-rgb), 0.6);
  --shadow-md: 0 18px 40px rgba(var(--shadow-rgb), 0.45);
  --shadow-sm: 0 12px 32px rgba(var(--shadow-rgb), 0.55);

  /* Product + status accents. Referenced from inline styles in the page
     components as well as from this sheet, so they must stay semantic. */
  --p-engram: #a78bfa;
  --p-doppler: #22d3ee;
  --p-immune: #f87171;
  --p-cloud: #fb923c;
  --p-pathway: #818cf8;
  --status-none: #6b7280;
  --status-none-rgb: 107, 114, 128;
  --warn-soft: #fcd34d;
  --tag-advanced: #f9a8d4;
  --tag-advanced-rgb: 244, 114, 182;

  /* Atmosphere strength (dialled back in light mode) */
  --glow-opacity: 0.7;

}
/* ── Figures ──────────────────────────────────────────────────────────
   Explanatory diagrams, drawn as inline SVG so they flip with the theme
   like everything else on the page. No literal colour is allowed in a
   figure: a group carries a *tone* (--fig-c plus its wash), and every
   shape and label inside reads those two variables. That is also why the
   arrowheads are drawn as polygons rather than <marker>s - a marker is
   resolved in the document, not in the group that points at it, so tone
   would leak from whichever figure happened to render first.

   On a narrow screen the figure scrolls rather than scaling down: SVG
   text shrinks with the viewBox, and a diagram you cannot read is worse
   than one you have to swipe. */
{
}
svg {
  display: block;
  font-family: var(--font-sans), system-ui, -apple-system, sans-serif;
  --fig-c: var(--text-faint);
  --fig-wash: rgba(var(--fg-rgb), 0.04);
}
figcaption {
  margin-top: 14px;
  font-family: var(--font-mono), ui-monospace, monospace;
  font-size: 12px;
  line-height: 1.65;
  letter-spacing: 0.02em;
  color: var(--text-faint);
  text-align: center;
}

/* Tones. Kept in step with the silhouette colours used on the Genesis
   page: Neurons accent, Engrams --p-engram, Effectors accent-3,
   Receptors accent-2. A component wears one colour across the suite. */
.t-neutral  { --fig-c: var(--text-faint);  --fig-wash: rgba(var(--fg-rgb), 0.04); }
.t-accent   { --fig-c: var(--accent);      --fig-wash: rgba(var(--accent-rgb), 0.10); }
.t-accent2  { --fig-c: var(--accent-2);    --fig-wash: rgba(var(--accent2-rgb), 0.10); }
.t-accent3  { --fig-c: var(--accent-3);    --fig-wash: rgba(var(--accent3-rgb), 0.10); }
.t-engram   { --fig-c: var(--p-engram);    --fig-wash: rgba(var(--engram-rgb), 0.10); }
.t-muted    { --fig-c: var(--status-none); --fig-wash: rgba(var(--status-none-rgb), 0.07); }

.fig-box   { fill: var(--fig-wash); stroke: var(--fig-c); stroke-width: 1.2; }
.fig-plain { fill: var(--bg-card);  stroke: var(--border-strong); stroke-width: 1; }
.fig-line  { fill: none; stroke: var(--fig-c); stroke-width: 1.3; }
.fig-hair  { fill: none; stroke: var(--border-strong); stroke-width: 1; }
.fig-dash  { stroke-dasharray: 5 5; }
.fig-fill  { fill: var(--fig-c); stroke: none; }
.fig-hollow{ fill: var(--bg); stroke: var(--fig-c); stroke-width: 1.4; }

.fig-title { fill: var(--text); font-size: 13.5px; font-weight: 600; }
.fig-sub   { fill: var(--text-dim); font-size: 11.5px; }
.fig-tag {
  fill: var(--fig-c);
  font-family: var(--font-mono), ui-monospace, monospace;
  font-size: 11px;
  letter-spacing: 0.04em;
}
.fig-note  { fill: var(--text-faint); font-size: 12px; font-style: italic; }
.fig-strike { stroke: var(--text-faint); stroke-width: 1.2; }

/* ── Figure motion ────────────────────────────────────────────────────
   A Signal on the bus is the one thing worth animating on this site, so
   the only moving part in a figure is a spark travelling a route.

   The route is passed in as custom properties rather than compiled into
   bespoke keyframes per diagram: --x0/--y0 through --x3/--y3 are read
   inside the keyframes, so three generic animations cover every path in
   every figure. Paths here are orthogonal - out of a node, along the bus,
   down into another - which is exactly what a sequence of translates
   draws, and translate needs no transform-origin to behave.

   Each spark also carries its start point as a plain inline transform, so
   when the animation is switched off it rests at the beginning of its
   route instead of collapsing into the top-left corner. */

@keyframes fig-run2 {
  0%   { transform: translate(var(--x0), var(--y0)); opacity: 0; }
  12%  { opacity: 1; }
  88%  { opacity: 1; }
  100% { transform: translate(var(--x1), var(--y1)); opacity: 0; }
}
@keyframes fig-run3 {
  0%   { transform: translate(var(--x0), var(--y0)); opacity: 0; }
  9%   { opacity: 1; }
  50%  { transform: translate(var(--x1), var(--y1)); }
  91%  { opacity: 1; }
  100% { transform: translate(var(--x2), var(--y2)); opacity: 0; }
}
@keyframes fig-run4 {
  0%   { transform: translate(var(--x0), var(--y0)); opacity: 0; }
  7%   { opacity: 1; }
  28%  { transform: translate(var(--x1), var(--y1)); }
  72%  { transform: translate(var(--x2), var(--y2)); }
  93%  { opacity: 1; }
  100% { transform: translate(var(--x3), var(--y3)); opacity: 0; }
}

/* A node acknowledging that something arrived. */
@keyframes fig-halo {
  0%   { transform: scale(0.55); opacity: 0.55; }
  70%  { opacity: 0; }
  100% { transform: scale(2.3); opacity: 0; }
}

/* A dashed edge that is carrying something rather than merely optional. */
@keyframes fig-flow {
  to { stroke-dashoffset: -20; }
}

.fig-spark,
.fig-spark-halo {
  animation-timing-function: linear;
  animation-iteration-count: infinite;
  animation-fill-mode: both;
}
.fig-spark { fill: var(--fig-c); }
.fig-spark-halo { fill: var(--fig-c); fill-opacity: 0.22; }

.fig-halo {
  fill: none;
  stroke: var(--fig-c);
  stroke-width: 1.2;
  transform-box: fill-box;
  transform-origin: center;
  animation-name: fig-halo;
  animation-timing-function: ease-out;
  animation-iteration-count: infinite;
  animation-fill-mode: both;
}

.fig-flow { animation: fig-flow 1.8s linear infinite; }

/* Motion is decoration here - the figure reads perfectly still. */
@media (prefers-reduced-motion: reduce) {
  svg * { animation: none !important; }
  .fig-spark-halo { opacity: 0; }
  .fig-halo { opacity: 0; }
}

/* ── Product miniatures ───────────────────────────────────────────────
   A few figures draw the actual tools at postage-stamp size: Genesis's
   canvas, a terminal, Prism's brain view. They need two type scales the
   rest of the kit does not - chrome (tab strips, window titles) and
   terminal output - and a way to make lines arrive in order. */
.fig-chrome {
  font-family: var(--font-mono), ui-monospace, monospace;
  font-size: 9px;
  letter-spacing: 0.03em;
  fill: var(--text-faint);
}
.fig-chrome.on { fill: var(--fig-c); }
.fig-term {
  font-family: var(--font-mono), ui-monospace, monospace;
  font-size: 9.5px;
  fill: var(--text-dim);
}
.fig-term.dim { fill: var(--text-faint); }
.fig-term.hot { fill: var(--fig-c); }

/* Output arriving a line at a time. Staggered by delay, looping with the
   sparks, so the terminal reads as a program that is still running. */
@keyframes fig-type {
  0%, 4%   { opacity: 0; }
  12%, 100% { opacity: 1; }
}
.fig-type {
  animation-name: fig-type;
  animation-timing-function: linear;
  animation-iteration-count: infinite;
  animation-fill-mode: both;
}

@keyframes fig-blink {
  0%, 49%   { opacity: 1; }
  50%, 100% { opacity: 0; }
}
.fig-caret {
  fill: var(--fig-c);
  animation: fig-blink 1.1s steps(1, end) infinite;
}


</style>
<rect width="100%" height="100%" fill="var(--bg)"/>
<title>Three windows side by side. The Genesis canvas shows a Synapse with a Neuron, an Engram, an Effector and a Receptor placed around it. A terminal runs brain.py and prints REGISTER, TASK, TOOL_CALL and FINAL lines. Prism&#x27;s Brain View shows the same four components around the same Synapse with Signals travelling between them. An arrow returns from Prism back to Genesis.</title><g class="t-accent3"><text class="fig-title" x="158" y="30" text-anchor="middle">Design</text><text class="fig-tag" x="158" y="46" text-anchor="middle">Genesis</text></g><rect x="18" y="62" width="280" height="200" rx="10" class="fig-plain"></rect><line x1="18" y1="86" x2="298" y2="86" class="fig-hair"></line><g class="t-accent3"><text class="fig-chrome on" x="32" y="78">Canvas</text></g><g class="t-accent3"><text class="fig-chrome" x="78" y="78">Code</text></g><g class="t-accent3"><text class="fig-chrome" x="124" y="78">Test</text></g><line x1="158" y1="174" x2="158" y2="128" class="fig-hair"></line><line x1="158" y1="174" x2="204" y2="174" class="fig-hair"></line><line x1="158" y1="174" x2="158" y2="220" class="fig-hair"></line><line x1="158" y1="174" x2="112" y2="174" class="fig-hair"></line><g class="t-accent2" transform="translate(158,174)"><circle r="17" class="fig-box"></circle><circle r="11" class="fig-line"></circle><circle r="4" class="fig-fill"></circle></g><g class="t-accent" transform="translate(158,122)"><circle r="8" class="fig-hollow"></circle></g><g class="t-engram" transform="translate(210,174)"><polygon points="0,-8 8,0 0,8 -8,0" class="fig-hollow"></polygon></g><g class="t-accent3" transform="translate(158,226)"><polygon points="0,-8 6.928,4 -6.928,4" class="fig-hollow"></polygon></g><g class="t-accent2" transform="translate(106,174)"><path d="M -8,0 A 8,8 0 0 0 8,0 L 4.96,0 A 4.96,4.96 0 0 1 -4.96,0 Z" class="fig-hollow"></path></g><g class="t-accent3"><rect x="32" y="232" width="72" height="18" rx="9" class="fig-box"></rect><text class="fig-chrome on" x="42" y="245">+ Neuron</text></g><text class="fig-chrome" x="284" y="245" text-anchor="end">neurons/planner.py</text><g class="t-accent"><text class="fig-title" x="460" y="30" text-anchor="middle">Run</text><text class="fig-tag" x="460" y="46" text-anchor="middle">Core</text></g><rect x="320" y="62" width="280" height="200" rx="10" class="fig-plain"></rect><line x1="320" y1="86" x2="600" y2="86" class="fig-hair"></line><text class="fig-chrome" x="334" y="78">brain.py</text><g class="t-accent"><circle cx="580" cy="75" r="3" class="fig-fill"></circle></g><g class="t-neutral"><text class="fig-term fig-type" x="334" y="112" style="animation-duration:6.4s;animation-delay:0s">$ python brain.py</text></g><g class="t-neutral"><text class="fig-term dim fig-type" x="334" y="134" style="animation-duration:6.4s;animation-delay:0.42s">synapse   cosmo://127.0.0.1:7070</text></g><g class="t-neutral"><text class="fig-term dim fig-type" x="334" y="156" style="animation-duration:6.4s;animation-delay:0.84s">REGISTER  planner · memory · search</text></g><g class="t-accent"><text class="fig-term hot fig-type" x="334" y="178" style="animation-duration:6.4s;animation-delay:1.26s">TASK      → planner</text></g><g class="t-accent"><text class="fig-term hot fig-type" x="334" y="200" style="animation-duration:6.4s;animation-delay:1.68s">TOOL_CALL → search</text></g><g class="t-accent"><text class="fig-term fig-type" x="334" y="222" style="animation-duration:6.4s;animation-delay:2.1s">FINAL     ok  1.4s</text></g><g class="t-accent"><rect x="334" y="234" width="5" height="11" class="fig-caret"></rect></g><g class="t-accent2"><text class="fig-title" x="762" y="30" text-anchor="middle">Observe</text><text class="fig-tag" x="762" y="46" text-anchor="middle">Prism</text></g><rect x="622" y="62" width="280" height="200" rx="10" class="fig-plain"></rect><line x1="622" y1="86" x2="902" y2="86" class="fig-hair"></line><g class="t-accent2"><text class="fig-chrome on" x="636" y="78">Brain</text></g><g class="t-accent2"><text class="fig-chrome" x="678" y="78">Tree</text></g><g class="t-accent2"><text class="fig-chrome" x="720" y="78">Metrics</text></g><line x1="740" y1="174" x2="740" y2="128" class="fig-hair"></line><line x1="740" y1="174" x2="786" y2="174" class="fig-hair"></line><line x1="740" y1="174" x2="740" y2="220" class="fig-hair"></line><line x1="740" y1="174" x2="694" y2="174" class="fig-hair"></line><g class="t-accent2" transform="translate(740,174)"><circle r="17" class="fig-box"></circle><circle r="11" class="fig-line"></circle><circle r="4" class="fig-fill"></circle></g><g class="t-accent" transform="translate(740,122)"><circle r="8" class="fig-hollow"></circle></g><g class="t-engram" transform="translate(792,174)"><polygon points="0,-8 8,0 0,8 -8,0" class="fig-hollow"></polygon></g><g class="t-accent3" transform="translate(740,226)"><polygon points="0,-8 6.928,4 -6.928,4" class="fig-hollow"></polygon></g><g class="t-accent2" transform="translate(688,174)"><path d="M -8,0 A 8,8 0 0 0 8,0 L 4.96,0 A 4.96,4.96 0 0 1 -4.96,0 Z" class="fig-hollow"></path></g><g class="t-accent2"><circle class="fig-spark-halo" r="7.0200000000000005" style="--x0:688px;--y0:174px;--x1:740px;--y1:174px;--x2:740px;--y2:122px;transform:translate(688px, 174px);animation-name:fig-run3;animation-duration:6.4s;animation-delay:0.6s"></circle><circle class="fig-spark" r="2.6" style="--x0:688px;--y0:174px;--x1:740px;--y1:174px;--x2:740px;--y2:122px;transform:translate(688px, 174px);animation-name:fig-run3;animation-duration:6.4s;animation-delay:0.6s"></circle></g><g class="t-accent3"><circle class="fig-spark-halo" r="7.0200000000000005" style="--x0:740px;--y0:122px;--x1:740px;--y1:174px;--x2:740px;--y2:226px;transform:translate(740px, 122px);animation-name:fig-run3;animation-duration:6.4s;animation-delay:2.4s"></circle><circle class="fig-spark" r="2.6" style="--x0:740px;--y0:122px;--x1:740px;--y1:174px;--x2:740px;--y2:226px;transform:translate(740px, 122px);animation-name:fig-run3;animation-duration:6.4s;animation-delay:2.4s"></circle></g><g class="t-accent3"><circle class="fig-spark-halo" r="7.0200000000000005" style="--x0:740px;--y0:226px;--x1:740px;--y1:174px;--x2:740px;--y2:122px;transform:translate(740px, 226px);animation-name:fig-run3;animation-duration:6.4s;animation-delay:4.2s"></circle><circle class="fig-spark" r="2.6" style="--x0:740px;--y0:226px;--x1:740px;--y1:174px;--x2:740px;--y2:122px;transform:translate(740px, 226px);animation-name:fig-run3;animation-duration:6.4s;animation-delay:4.2s"></circle></g><line x1="844" y1="86" x2="844" y2="262" class="fig-hair"></line><g class="t-accent2"><rect x="852" y="100" width="38" height="7" rx="3.5" class="fig-box"></rect></g><g class="t-accent"><rect x="852" y="120" width="28" height="7" rx="3.5" class="fig-box"></rect></g><g class="t-accent3"><rect x="852" y="140" width="28" height="7" rx="3.5" class="fig-box"></rect></g><g class="t-engram"><rect x="852" y="160" width="38" height="7" rx="3.5" class="fig-box"></rect></g><g class="t-accent"><rect x="852" y="180" width="28" height="7" rx="3.5" class="fig-box"></rect></g><g class="t-accent2"><rect x="852" y="200" width="28" height="7" rx="3.5" class="fig-box"></rect></g><g class="t-accent"><rect x="852" y="220" width="38" height="7" rx="3.5" class="fig-box"></rect></g><g class="t-accent2"><path d="M762 262 L762 312 L158 312 L158 271" class="fig-line"></path><polygon class="fig-fill" points="158,262 163,276 153,276"></polygon></g><g class="t-accent2"><circle class="fig-spark-halo" r="7.56" style="--x0:762px;--y0:266px;--x1:762px;--y1:312px;--x2:158px;--y2:312px;--x3:158px;--y3:266px;transform:translate(762px, 266px);animation-name:fig-run4;animation-duration:6.4s;animation-delay:5s"></circle><circle class="fig-spark" r="2.8" style="--x0:762px;--y0:266px;--x1:762px;--y1:312px;--x2:158px;--y2:312px;--x3:158px;--y3:266px;transform:translate(762px, 266px);animation-name:fig-run4;animation-duration:6.4s;animation-delay:5s"></circle></g><g class="t-accent2"><text class="fig-tag" x="460" y="298" text-anchor="middle">what you watch sends you back to the canvas</text></g>
</svg>
