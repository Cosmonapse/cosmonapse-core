// Replaced by StartScreen, which probes each folder as you browse it and
// offers to open what's already there instead of only scaffolding something
// new. Kept as a re-export so nothing breaks on a stale import; safe to
// delete.
export { StartScreen as StartForm } from "./StartScreen";
